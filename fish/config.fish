if status is-interactive
    # Commands to run in interactive sessions can go here
end

alias vim='nvim'
alias np='cd ~/projects && nvim'
alias config='cd ~/.config'
alias home='cd ~'
alias cls='clear'
alias notes='cd storage/downloads/Vault/'
alias books='cd storage/downloads/Books/'
